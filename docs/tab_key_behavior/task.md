# タブ入力および設定機能のタスクリスト

- [x] 設定画面 (HTML) の拡張
  - [x] `index.html` にタブ挙動選択用の `<select>` ドロップダウンを追加
- [x] バックエンド (Rust) の設定拡張
  - [x] `main.rs` の `AppSettings` 構造体に `tab_behavior` フィールド（デフォルト値あり）を追加
  - [x] `SettingsResponse` 構造体へのフィールド追加
  - [x] `get_settings` コマンドで設定返却処理の修正
  - [x] `save_settings` コマンドの引数および保存処理の修正
- [x] フロントエンド (JS) の設定処理拡張
  - [x] `main.js` の `appState` に `tabBehavior` を追加
  - [x] `init` 関数で `tabBehavior` の読み込みとセレクトボックスへの値反映処理を追加
  - [x] `saveSettings` / `saveSettingsDelay` 関数で `tabBehavior` を保存パラメータに追加
- [x] エディタ要素のキーボード制御の実装
  - [x] `keydown` イベントリスナーを追加し、`Tab` キー押下をハンドリング
  - [x] 単一行インデント処理の実装（選択位置への文字挿入）
  - [x] 複数行インデント処理の実装（選択された各行の先頭への文字挿入）
  - [x] Shift+Tabによる逆インデント処理の実装（選択された各行の先頭からの文字削除）
  - [x] 操作後のカーソル選択範囲の再計算・調整ロジック実装
  - [x] エディタ内容更新時の `input` イベント発火処理の実装
- [x] 動作確認・テスト
  - [x] 設定変更・再起動時の永続化確認
  - [x] 各インデントタイプ（タブ/スペース2/スペース4）での入力確認
  - [x] 単一行/複数行選択時の Tab インデント動作確認
  - [x] 単一行/複数行選択時の Shift+Tab 逆インデント動作確認

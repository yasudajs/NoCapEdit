# タブ入力および設定機能のタスクリスト

- [ ] 設定画面 (HTML) の拡張
  - [ ] `index.html` にタブ挙動選択用の `<select>` ドロップダウンを追加
- [ ] バックエンド (Rust) の設定拡張
  - [ ] `main.rs` の `AppSettings` 構造体に `tab_behavior` フィールド（デフォルト値あり）を追加
  - [ ] `SettingsResponse` 構造体へのフィールド追加
  - [ ] `get_settings` コマンドで設定返却処理の修正
  - [ ] `save_settings` コマンドの引数および保存処理の修正
- [ ] フロントエンド (JS) の設定処理拡張
  - [ ] `main.js` の `appState` に `tabBehavior` を追加
  - [ ] `init` 関数で `tabBehavior` の読み込みとセレクトボックスへの値反映処理を追加
  - [ ] `saveSettings` / `saveSettingsDelay` 関数で `tabBehavior` を保存パラメータに追加
- [ ] エディタ要素のキーボード制御の実装
  - [ ] `keydown` イベントリスナーを追加し、`Tab` キー押下をハンドリング
  - [ ] 単一行インデント処理の実装（選択位置への文字挿入）
  - [ ] 複数行インデント処理の実装（選択された各行の先頭への文字挿入）
  - [ ] Shift+Tabによる逆インデント処理の実装（選択された各行の先頭からの文字削除）
  - [ ] 操作後のカーソル選択範囲 of 再計算・調整ロジック実装
  - [ ] エディタ内容更新時の `input` イベント発火処理の実装
- [ ] 動作確認・テスト
  - [ ] 設定変更・再起動時の永続化確認
  - [ ] 各インデントタイプ（タブ/スペース2/スペース4）での入力確認
  - [ ] 単一行/複数行選択時の Tab インデント動作確認
  - [ ] 単一行/複数行選択時の Shift+Tab 逆インデント動作確認

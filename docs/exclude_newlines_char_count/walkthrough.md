# 文字数カウントの改行除外対応 ウォークスルー

## 修正内容
* [src/dist/main.js](file:///c:/work/NoCapEdit/src/dist/main.js)
  * `updateEditorMetrics` 関数内の文字数計算ロジックを変更しました。
  * `value.length` を `value.replace(/\r?\n/g, '').length` に変更し、改行コードを除外した純粋な文字数をカウントしてステータスバーに表示するようにしました。

## 動作確認結果
* プロジェクトのビルド（`cargo build`）が正常に通ることを確認しました。
* 実際の挙動については、エディタを起動し、テキストを入力した状態で改行を行った際に、文字数カウントが加算されないことを確認してください。

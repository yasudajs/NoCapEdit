# タスク：設定画面閉じた後のカーソル位置復元 ＆ タブ切り替え時の状態復元

- [x] 設定画面開閉時のフォーカス・カーソル位置復元実装
    - [x] `main.js` に `savedEditorCursor` 変数を定義
    - [x] `openSettingsDialog` でエディタ位置情報の記憶処理を追加（改善案の存在チェック付き）
    - [x] `closeSettingsDialog` でエディタ位置情報の復帰とフォーカス処理を追加（改善案の存在チェック付き）
- [ ] 新規ウィンドウ/タブ追加時の自動フォーカス ＆ タブ切り替え時の状態復元実装
    - [ ] `switchTab` で切り替え前のタブのカーソル状態を記憶する処理を追加
    - [ ] `switchTab` で切り替え後のタブのカーソル状態の復元と自動フォーカス処理を追加（新規タブ時の末尾フォーカスも含む）
- [ ] 動作検証
    - [ ] 1. 【設定画面】基本的なカーソル復元テスト（閉じた後にフォーカスと位置が復帰するか）
    - [ ] 2. 【設定画面】選択状態およびスクロール位置の復帰テスト
    - [ ] 3. 【新規起動】起動直後にエディタへ自動的にフォーカスとカーソルが入るか
    - [ ] 4. 【タブ追加】新規タブ作成直後に自動フォーカスされカーソルが入るか
    - [ ] 5. 【タブ切替】既存タブ間の切り替え時に、それぞれの直前のカーソル位置・スクロール位置が復元されるか

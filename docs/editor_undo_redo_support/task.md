# タスクリスト: エディタ操作（行操作・インデント）のUndo/Redo（Ctrl+Z / Ctrl+Y）対応

- [x] **1. バージョン番号の更新（4ファイル一括更新）**
  - [x] `Cargo.toml`: `0.1.78`
  - [x] `tauri.conf.json`: `0.1.78`
  - [x] `nsis/installer.nsi`: `0.1.78.0`
  - [x] `docs/DEVELOPMENT.md`: `0.1.78`
- [x] **2. 仕様書・ドキュメントの更新**
  - [x] `docs/spec.md`: Undo/Redo 連動仕様を明記
- [x] **3. Undo対応テキスト置換ヘルパーの実装と各操作の改修**
  - [x] `src/dist/js/ui/editor.js`:
    - `applyEditorTextWithUndo` ヘルパーの実装（`document.execCommand` 活用）
    - `moveLine` の改修
    - `duplicateLine` の改修
    - `deleteLine` の改修
    - `handleTabKey`（インデント・アンインデント）の改修
- [x] **4. 検証**
  - [x] ビルド確認（`cargo check` / `cargo test`）
  - [x] 行削除 (`Ctrl + Shift + K`) 後の `Ctrl + Z` / `Ctrl + Y` 動作確認
  - [x] 行移動 (`Alt + ↑ / ↓`) 後の `Ctrl + Z` / `Ctrl + Y` 動作確認
  - [x] 行複製 (`Alt + Shift + ↑ / ↓`) 後の `Ctrl + Z` / `Ctrl + Y` 動作確認
  - [x] インデント (`Tab` / `Shift + Tab`) 後の `Ctrl + Z` / `Ctrl + Y` 動作確認
  - [x] 通常の文字入力と行操作を混在させた連続 Undo/Redo の動作確認
  - [x] 複数行選択時の Undo/Redo 動作確認
- [x] **5. 完了報告ドキュメント作成**
  - [x] `docs/editor_undo_redo_support/walkthrough.md` の作成
  - [x] `docs/history.md` に `0.1.78` の変更履歴を追記
  - [x] コミット＆プッシュ

- [x] 実装計画の承認
- [x] `docs/spec.md` の更新
- [ ] `index.html` の改修
  - [ ] 設定ボタン（`#settingsBtn`）に✕アイコンを追加
  - [ ] OKボタン（`#confirmSettingsBtn`）と親要素（`.dialog-actions`）の削除
- [ ] `style.css` の改修
  - [ ] 右ドック化のスタイル（`transform`、`width`、`height: 100%` など）
  - [ ] 設定ボタンのアイコン切り替えスタイル
  - [ ] ドック内のスクロール（`overflow-y: auto`）対応
  - [ ] トグル開閉用のアニメーション（`.open` クラスの制御）
- [ ] `main.js` の改修
  - [ ] 設定ボタンクリック時のトグル開閉処理（`.open` の着脱）実装
  - [ ] フォルダ選択時および各セレクトボックス（保存モードなど）変更時の即時保存処理の実装
  - [ ] OKボタン関連の処理の削除
  - [ ] 保存モード切替時の特殊処理（ファイル作成・削除）の即時反映化
- [ ] ユーザーによる手動テスト
  - [ ] 歯車ボタンをクリックして、右側から設定ドックがスライドイン・アウトするか確認する
  - [ ] ドック展開時、設定ボタンのアイコンが「✕」に変わるか確認する
  - [ ] ドック内で「テーマ」や「保存モード」を変更した際、即座に画面・ステータスバー等へ反映されるか確認する
  - [ ] ウィンドウサイズを縦に小さくした際、設定ドック内で縦スクロールが可能か確認する

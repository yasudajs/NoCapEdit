- [x] 実装計画の承認
- [x] `docs/spec.md` の更新
- [x] `index.html` の改修
  - [x] 設定ボタン（`#settingsBtn`）に✕アイコンを追加
  - [x] OKボタン（`#confirmSettingsBtn`）と親要素（`.dialog-actions`）の削除
- [x] `style.css` の改修
  - [x] 右ドック化のスタイル（`transform`、`width`、`height: 100%` など）
  - [x] 設定ボタンのアイコン切り替えスタイル
  - [x] ドック内のスクロール（`overflow-y: auto`）対応
  - [x] トグル開閉用のアニメーション（`.open` クラスの制御）
- [x] `main.js` の改修
  - [x] 設定ボタンクリック時のトグル開閉処理（`.open` の着脱）実装
  - [x] フォルダ選択時および各セレクトボックス（保存モードなど）変更時の即時保存処理の実装
  - [x] OKボタン関連の処理の削除
  - [x] 保存モード切替時の特殊処理（ファイル作成・削除）の即時反映化
- [x] ユーザーによる手動テスト
  - [x] 歯車ボタンをクリックして、右側から設定ドックがスライドイン・アウトするか確認する
  - [x] ドック展開時、設定ボタンのアイコンが「✕」に変わるか確認する
  - [x] ドック内で「テーマ」や「保存モード」を変更した際、即座に画面・ステータスバー等へ反映されるか確認する
  - [x] ウィンドウサイズを縦に小さくした際、設定ドック内で縦スクロールが可能か確認する

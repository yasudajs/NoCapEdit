# タスクリスト: 設定保存処理のDRY化

- [ ] 設定ファイルの確認と調査
- [x] バージョン番号の更新（0.1.30 -> 0.1.31）
  - [x] `Cargo.toml` のバージョンを `0.1.31` に更新
  - [x] `tauri.conf.json` のバージョンを `0.1.31` に更新
  - [x] `nsis/installer.nsi` のバージョンを `0.1.31` に更新
  - [x] `docs/DEVELOPMENT.md` のビルド手順内のバージョンを `0.1.31` に更新
  - [x] `docs/history.md` に `Ver 0.1.31` のセクションを追記
- [x] Rust側: `save_settings` コマンドの修正
  - [x] `main.rs` の `save_settings` の引数を `AppSettings` 構造体に一本化する
  - [x] 余分なメンバ変数詰め替え処理の削除
- [x] JS側: 設定保存ラッパー関数の作成と適用
  - [x] `main.js` に `saveApplicationSettings` 関数を新規作成し、最新の `appState` から設定オブジェクトを組み立てて `save_settings` を呼び出すようにする
  - [x] 設定画面保存処理（`saveSettings` 内）の `invoke` 呼び出しを共通関数に置き換える
  - [x] テーマ切り替え処理（`toggleTheme` 内）の `invoke` 呼び出しを共通関数に置き換える
  - [x] 遅延保存処理（`saveSettingsDelay` 内）の `invoke` 呼び出しを共通関数に置き換える
- [x] 動作検証
  - [x] ビルドが正常に通ることを確認
  - [x] テーマの切り替えが正しく保存・復元されることを確認
  - [x] 設定画面からの変更（フォントファミリー、インデント設定、保存モード等）が正しく保存・復元されることを確認
  - [x] 自動保存（遅延保存）が正常に動作することを確認
- [/] クリーンアップと報告
  - [x] ドキュメントを `docs/wip/dry_save_settings/` から `docs/dry_save_settings/` へ移動（完了済み）
  - [/] コミットおよびプッシュ

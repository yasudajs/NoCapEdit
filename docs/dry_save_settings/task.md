# タスクリスト: 設定保存処理のDRY化

- [ ] 設定ファイルの確認と調査
- [ ] バージョン番号の更新（0.1.30 -> 0.1.31）
  - [ ] `Cargo.toml` のバージョンを `0.1.31` に更新
  - [ ] `tauri.conf.json` のバージョンを `0.1.31` に更新
  - [ ] `nsis/installer.nsi` のバージョンを `0.1.31` に更新
  - [ ] `docs/DEVELOPMENT.md` のビルド手順内のバージョンを `0.1.31` に更新
  - [ ] `docs/history.md` に `Ver 0.1.31` のセクションを追記
- [ ] Rust側: `save_settings` コマンドの修正
  - [ ] `main.rs` の `save_settings` の引数を `AppSettings` 構造体に一本化する
  - [ ] 余分なメンバ変数詰め替え処理の削除
- [ ] JS側: 設定保存ラッパー関数の作成と適用
  - [ ] `main.js` に `saveApplicationSettings` 関数を新規作成し、最新の `appState` から設定オブジェクトを組み立てて `save_settings` を呼び出すようにする
  - [ ] 設定画面保存処理（`saveSettings` 内）の `invoke` 呼び出しを共通関数に置き換える
  - [ ] テーマ切り替え処理（`toggleTheme` 内）の `invoke` 呼び出しを共通関数に置き換える
  - [ ] 遅延保存処理（`saveSettingsDelay` 内）の `invoke` 呼び出しを共通関数に置き換える
- [ ] 動作検証
  - [ ] ビルドが正常に通ることを確認
  - [ ] テーマの切り替えが正しく保存・復元されることを確認
  - [ ] 設定画面からの変更（フォントファミリー、インデント設定、保存モード等）が正しく保存・復元されることを確認
  - [ ] 自動保存（遅延保存）が正常に動作することを確認
- [ ] クリーンアップと報告
  - [ ] ドキュメントを `docs/wip/dry_save_settings/` から `docs/dry_save_settings/` へ移動（本番格上げ）
  - [ ] コミットおよびプッシュ

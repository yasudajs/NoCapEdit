# タスクリスト - ステータスバーからのファイル名表示削除とフォントサイズ表示追加

- [x] 1. メタデータのバージョン更新
  - [x] Cargo.toml のバージョンを 0.1.8 に更新
  - [x] tauri.conf.json のバージョンを 0.1.8 に更新
- [x] 2. フロントエンド UI および Rust の修正
  - [x] index.html から `statusFile` 要素を削除、タイトルバーのバージョンを 0.1.8 に更新
  - [x] style.css から `.status-file` スタイルの定義を削除
  - [x] main.js から `statusFile` 参照、`updateStatusFileLabel` 関数およびその呼び出し箇所を削除
  - [x] main.js の `updateEditorMetrics()` 内にフォントサイズ表示（`| ${appState.fontSize} pt`）を追加
  - [x] main.js の `applyFontSize()` で `updateEditorMetrics()` を呼び出すように修正
  - [x] main.rs の WindowBuilder.title にハードコードされているバージョンを 0.1.8 に更新
- [x] 3. 動作確認・検証
  - [x] アプリを起動してステータスバー右下のファイル名表示が消えていることを確認
  - [x] ステータスバー右下にフォントサイズが `Ln X, Col Y | Z chars | xx pt` 形式で表示されていることを確認
  - [x] フォントサイズ変更時にステータスバー表示が即座に追従することを確認
  - [x] タイトルバーのバージョンが `NoCapEdit [ Ver 0.1.8 ]` と表示されることを確認
  - [x] タブ切り替え、ファイル保存等の操作に影響がないことを確認
- [x] 4. コミットとプッシュ
  - [x] 修正内容の確認 (walkthrough.md) を作成
  - [x] git commit & push (ユーザーの承認を得てから)

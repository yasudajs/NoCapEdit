# フェーズ 1-3: 正規表現処理の完全削除および連番ロジックの統一 タスクリスト

- [ ] `src/main.rs` の `move_file_or_dir` および `copy_file_or_dir` から、前回の `OnceLock` 実装と `Regex` による切り落とし処理を完全に削除する
- [ ] 同関数において、連番のゼロ埋めを廃止する（例: `{:02}` を `{}` に変更）
- [ ] 同関数において、連番の探索ループ (`while dest_path.exists()`) に上限 (9) を設定し、超えたらエラーを返すようにする
- [ ] ビルドテスト (`cargo check` および `cargo build`) を実行してエラーが出ないことを確認する
- [ ] 【ユーザー依頼】手動テストを行い、期待通り `abcd_01_1` となること、および上限チェックが機能することを確認する
- [ ] テスト完了後、前回のコミットを修正（または新しいコミットとして追加）し、`docs/history.md` に履歴を追記して最終プッシュを行う

# タスクリスト - サイドバーのキーボードナビゲーション & ファイルコピペ機能 (Phase 5)

- [x] バックエンド (Rust) でファイル/ディレクトリ複製コマンドを追加 (`copy_file_or_dir`)
  - [x] ディレクトリ再帰コピーのヘルパー関数実装
  - [x] 連番衝突回避処理の実装
  - [x] セキュリティチェックの実装
  - [x] コマンドの登録
- [x] フロントエンド (JavaScript) でフォーカス移動の実装
  - [x] `Ctrl + E` グローバルキーハンドラの設定
  - [x] `focusSidebarTree()` の実装と、アクティブタブ実ファイルの Reveal 処理
  - [x] ツリーフォーカス時に `Esc` や `Enter` でエディタへフォーカスを戻す処理
- [x] フロントエンドでキーボードナビゲーションの実装
  - [x] 矢印キー（上下）による可視ツリー項目のフォーカス＆選択移動
  - [x] 矢印キー（左右）によるフォルダ展開・折りたたみ・親フォルダへの移動
  - [x] `Enter` キーによる決定処理（ファイルを開く・フォルダトグル）の `Space` からの移行
- [x] フロントエンドでツリー操作ショートカットの実装
  - [x] `Ctrl + N` / `Ctrl + D` による新規作成のインライン入力トリガー
  - [x] `F2` による名前変更のインライン入力トリガー
  - [x] `Ctrl + C` によるパス記憶とコピー状態表示
  - [x] `Ctrl + V` による複製APIの呼び出しとツリーの再描画
- [x] バックエンド (Rust) でファイル移動コマンド (`move_file_or_dir`) の衝突回避（連番）対応
- [x] フロントエンド (JavaScript) で切り取り・貼り付け（移動）機能の実装
  - [x] `clipboardState`（path, isDir, mode）の定義と状態管理
  - [x] `Ctrl + X` での切り取りおよび対象アイテムの半透明化 (`cut-pending`)
  - [x] `Esc` による切り取りのキャンセル（半透明化解除＆クリップボードクリア）
  - [x] `Ctrl + V` での切り取り貼り付け（移動）の実行と後処理
- [x] フロントエンドでフォーカス状態遷移の一元管理の実装
  - [x] `focusin` / `focusout` イベントによる青/グレー（`selected` / `selected-inactive`）表示 of 切り替え
  - [x] 新規作成（Ctrl+N/D）確定時のエディタフォーカス遷移およびキャンセル時のツリーへのフォーカス復帰
  - [x] 名前変更（F2）確定/キャンセル時のツリーアイテムへのフォーカス復帰
  - [x] 削除（Delete）実行時の隣接アイテムまたは親フォルダへの自動フォーカス遷移
- [x] ドキュメント・履歴の更新
  - [x] spec.md の更新
  - [x] implementation_plan.md の更新
  - [x] history.md の更新
- [x] 動作確認と検証
  - [x] `cargo check` および `cargo build` によるビルド確認
  - [x] 手動テスト（検証）の実施
    - [x] 1. フォーカス移動と基本遷移
      - [x] 起動時に新規タブ（名称未設定.nctx）が開き、すぐにエディタに入力できること
      - [x] ファイルツリーからファイルを開いた（クリックやEnter）際、エディタにフォーカスが移動すること
      - [x] ツリー操作中（青色）に `Esc` を押した際、エディタにフォーカスが移動すること
    - [x] 2. グローバル新規作成 (`Ctrl+N` / `Ctrl+D`)
      - [x] エディタ入力中、ツリー非表示状態で `Ctrl+N` / `Ctrl+D` を押した際、ツリーが自動で開き、ホームフォルダ直下にインライン新規作成が開始されるか
      - [x] 新規作成確定時にファイルが開きエディタへフォーカスが移動するか、キャンセル時に親フォルダ等にフォーカスが戻るか
    - [x] 3. 重複名存在時の連番処理
      - [x] コピーまたは移動先に同名が存在する場合、`_copy` を挟まずに初回から `_01` 等の連番が自動付与されるか
      - [x] 連番付きのファイルをさらにコピペした際、末尾 of 連番がインクリメント（`_02`など）されて複製されるか
    - [x] 4. コピー・切り取り＆貼り付け
      - [x] ファイル/フォルダ選択中、`Ctrl+X` で対象が半透明（透明度50%）に表示されるか
      - [x] 切り取り中に `Esc` キーを押すことで、切り取りがキャンセルされ、半透明表示が元に戻るか
      - [x] 切り取り中に別のフォルダを選択して `Ctrl+V` を押した際、ファイルが「移動」し、元のファイルが消えて移動先に配置されるか
      - [x] 切り取り貼り付けが完了した際、半透明表示がクリアされるか
      - [x] 切り取り中にコピー（`Ctrl+C`）や別の切り取り（`Ctrl+X`）を行った際、以前のアイテムの半透明化が解除されるか
      - [x] コピー貼り付け（`Ctrl+C` ➔ `Ctrl+V`）または切り取り移動の完了後、新しく貼り付けられたファイル/フォルダに自動で選択＋フォーカス（青色）が当たり、連続した操作がスムーズに行えるか
    - [x] 5. 削除時（Delete / Shift+Delete）のフォーカス継続
      - [x] ファイルを削除した際、自動で「次の兄弟」「前の兄弟」「親フォルダ」の順にフォーカスが移動し、キー操作が途切れないか確認
    - [x] 6. 名前変更時（F2）のフォーカス継続
      - [x] 名前変更完了またはキャンセル時に、対象ファイルにフォーカスが戻るか確認
    - [x] 7. 保存完了後のツリーの選択表示状態
      - [x] ファイルツリーからファイルを開いてエディタで編集し、保存（自動/手動）を実行した際、ツリーの該当ファイルの背景色が青（フォーカス状態）ではなく、グレー（非アクティブ選択状態）のままで維持されるか

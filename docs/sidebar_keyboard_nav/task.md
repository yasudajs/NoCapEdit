# タスクリスト - サイドバーのキーボードナビゲーション & ファイルコピペ機能 (Phase 5)

- [x] バックエンド (Rust) でファイル/ディレクトリ複製コマンドを追加 (`copy_file_or_dir`)
  - [x] ディレクトリ再帰コピーのヘルパー関数実装
  - [x] 連番衝突回避処理の実装
  - [x] セキュリティチェックの実装
  - [x] コマンドの登録
- [x] フロントエンド (JavaScript) でフォーカス移動の実装
  - [x] `Ctrl + E` グローバルキーハンドラの設定
  - [x] `focusSidebarTree()` の実装と、アクティブタブ実ファイルの Reveal 処理
  - [x] ツリーフォーカス時に `Esc` や `Enter` でエディタへフォーカスを戻す処理
- [x] フロントエンドでキーボードナビゲーションの実装
  - [x] 矢印キー（上下）による可視ツリー項目のフォーカス＆選択移動
  - [x] 矢印キー（左右）によるフォルダ展開・折りたたみ・親フォルダへの移動
  - [x] `Enter` キーによる決定処理（ファイルを開く・フォルダトグル）の `Space` からの移行
- [x] フロントエンドでツリー操作ショートカットの実装
  - [x] `Ctrl + N` / `Ctrl + D` による新規作成のインライン入力トリガー
  - [x] `F2` による名前変更のインライン入力トリガー
  - [x] `Ctrl + C` によるパス記憶とコピー状態表示
  - [x] `Ctrl + V` による複製APIの呼び出しとツリーの再描画
- [ ] バックエンド (Rust) でファイル移動コマンド (`move_file_or_dir`) の衝突回避（連番）対応
- [ ] フロントエンド (JavaScript) で切り取り・貼り付け（移動）機能の実装
  - [ ] `clipboardState`（path, isDir, mode）の定義と状態管理
  - [ ] `Ctrl + X` での切り取りおよび対象アイテムの半透明化 (`cut-pending`)
  - [ ] `Esc` による切り取りのキャンセル（半透明化解除＆クリップボードクリア）
  - [ ] `Ctrl + V` での切り取り貼り付け（移動）の実行と後処理
- [x] ドキュメント・履歴の更新
  - [x] spec.md の更新
  - [x] implementation_plan.md の更新
  - [x] history.md の更新
- [ ] 動作確認と検証
  - [ ] `cargo check` および `cargo build` によるビルド確認
  - [ ] 手動テスト（検証）の実施
    - [ ] 1. フォーカス移動 (`Ctrl+E` / `Esc` / `Enter`)
      - [ ] エディタ入力中、ツリー非表示状態で `Ctrl+E` を押した際、ツリーが自動で開き、現在アクティブなファイルに Reveal（展開・スクロール・フォーカス）されるか
    - [ ] 2. グローバル新規作成 (`Ctrl+N` / `Ctrl+D`)
      - [ ] エディタ入力中やツリー非表示の状態で `Ctrl+N` / `Ctrl+D` を押した際、ツリーが自動で開き、ホームフォルダ直下にインライン新規作成が開始されるか
    - [ ] 3. 重複名存在時の連番処理
      - [ ] コピーまたは移動先に同名が存在する場合、`_copy` を挟まずに初回から `_01` 等の連番が自動付与されるか
      - [ ] 連番付きのファイルをさらにコピペした際、末尾の連番がインクリメント（`_02`など）されて複製されるか
    - [ ] 4. 切り取り＆貼り付け (`Ctrl+X` / `Ctrl+V` / `Esc`)
      - [ ] ファイル/フォルダ選択中、`Ctrl+X` で対象が半透明（透明度50%）に表示されるか
      - [ ] 切り取り中に `Esc` キーを押すことで、切り取りがキャンセルされ、半透明表示が元に戻るか
      - [ ] 切り取り中に別のフォルダ（またはファイル）を選択して `Ctrl+V` を押した際、ファイルが「移動」し、元のファイルが消えて移動先に配置されるか
      - [ ] 切り取り貼り付けが完了した際、半透明表示がクリアされるか
      - [ ] 切り取り貼り付け完了後、さらに `Ctrl+V` を押しても再ペーストされないこと（1回限りの動作）を確認
      - [ ] 切り取り中にコピー（`Ctrl+C`）や別の切り取り（`Ctrl+X`）を行った際、以前のアイテムの半透明化が解除されるか

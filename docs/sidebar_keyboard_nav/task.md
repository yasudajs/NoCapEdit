# タスクリスト - サイドバーのキーボードナビゲーション & ファイルコピペ機能 (Phase 5)

- [x] バックエンド (Rust) でファイル/ディレクトリ複製コマンドを追加 (`copy_file_or_dir`)
  - [x] ディレクトリ再帰コピーのヘルパー関数実装
  - [x] 連番衝突回避処理の実装
  - [x] セキュリティチェックの実装
  - [x] コマンドの登録
- [x] フロントエンド (JavaScript) でフォーカス移動の実装
  - [x] `Ctrl + E` グローバルキーハンドラの設定
  - [x] `focusSidebarTree()` の実装と、アクティブタブ実ファイルの Reveal 処理（自動フォルダ展開＋スクロール）
  - [x] ツリーフォーカス時に `Esc` や `Enter` でエディタへフォーカスを戻す処理
- [x] フロントエンドでキーボードナビゲーションの実装
  - [x] 矢印キー（上下）による可視ツリー項目のフォーカス＆選択移動
  - [x] 矢印キー（左右）によるフォルダ展開・折りたたみ・親フォルダへの移動
  - [x] `Enter` キーによる決定処理（ファイルを開く・フォルダトグル）の `Space` からの移行
- [x] フロントエンドでツリー操作ショートカットの実装
  - [x] `Ctrl + N` / `Ctrl + D` による新規作成のインライン入力トリガー
  - [x] `F2` による名前変更のインライン入力トリガー
  - [x] `Ctrl + C` によるパス記憶とステータスバーへのコピー状態表示
  - [x] `Ctrl + V` による複製APIの呼び出しとツリーの再描画・選択クリア
- [x] 動作確認と検証
  - [x] `cargo check` および `cargo build` によるビルド確認
  - [x] フォーカス移動、ツリー操作、コピペ機能の統合テスト
- [/] 手動テスト（検証）の実施
  - [/] 1. フォーカス移動 (`Ctrl+E` / `Esc` / `Enter`)
    - [/] エディタ入力中、`Ctrl+E` でサイドバーツリーの現在編集中の実ファイルへフォーカスが移動し、ツリーが自動展開＆スクロールされるか
    　→　ファイルツリーを閉じている状態だと、ファイルツリーが開かないのでフォーカスがあたっているかわからない
    　→　ファイルツリーを閉じている状態でCtrl+Eを押下したら、ファイルツリーを開くようにする
    - [x] 新規未保存タブ（実ファイル未作成）の時、`Ctrl+E` でツリーの先頭項目にフォーカスが当たるか
    - [x] ツリーフォーカス中、`Esc` でエディタにフォーカスが戻るか
    - [x] ツリーフォーカス中、ファイルを選択して `Enter` でファイルが開き、かつフォーカスがエディタへ移るか
  - [x] 2. キーボードナビゲーション (矢印キー / `Enter`)
    - [x] `ArrowUp` / `ArrowDown` で、展開されているフォルダの中身を含めて正しく順次フォーカス移動できるか
    - [x] `ArrowRight` で、閉じているフォルダを展開できるか
    - [x] `ArrowRight` で、展開済みのフォルダの最初の子要素にフォーカス移動できるか
    - [x] `ArrowLeft` で、展開されているフォルダを折りたためるか
    - [x] `ArrowLeft` で、閉じているフォルダまたはファイル選択時に親フォルダを選択移動できるか
    - [x] `Enter` で、フォルダの展開・折りたたみのトグル動作が正常か
    - [x] `Space` キーでファイルが開かないことを確認（操作除外）
  - [/] 3. 新規作成・名前変更ショートカット
    - [x] ツリーフォーカス中、`Ctrl+N` で選択フォルダ配下に新規ファイルのインライン入力が起動し、作成できるか
    - [x] ツリーフォーカス中、`Ctrl+D` で選択フォルダ配下に新規フォルダのインライン入力が起動し、作成できるか
    　→ ファイルツリーを閉じている状態だと機能しない
    　→　ファイルやフォルダを選択していればCtrl+N/Dは機能するが、ホームフォルダにファイルやディレクトリを作ろうとして、ツリーの空き部分をクリックして(ファイルやディレクトリからフォーカスが外れた状態で)Ctrl+N/Dを押下しても機能しない
    　→　ファイルツリーを閉じた状態でCtrl+Nを押下したら、ファイルツリーを開いて、ホームフォルダを対象として、新規ファイル作成にする
    　→　ファイルツリーを閉じた状態でCtrl+Dを押下したら、ファイルツリーを開いて、ホームフォルダを対象として、新規フォルダ作成にする
    - [x] ツリーフォーカス中、`F2` で選択項目のインライン名前変更が起動し、変更できるか
  - [x] 4. コピー＆ペースト (`Ctrl+C` / `Ctrl+V`)
    - [x] ツリーフォーカス中、項目を選択して `Ctrl+C` でパスがコピーされる（ステータスバー表示）か
    - [x] 別のフォルダ（またはファイル）を選択して `Ctrl+V` で複製が貼り付けられるか
    - [x] 同一フォルダに貼り付けた場合、`xxx_copy` や `xxx_copy_01` 等で重複を自動退避して複製されるか
    　→　複数回コピーを繰り返すと_copyがどんどん増えていくので、_01などの連番処理にしたい。
    - [x] フォルダを `Ctrl+C` して `Ctrl+V` 貼付した時、子要素も再帰的に複製されるか
    - [/] コピー元のフォルダ自身やそのサブフォルダ配下へペーストしようとした際、エラーとなり移動がキャンセルされるか
      →　_cpopyが付与されるのでエラーにはならない



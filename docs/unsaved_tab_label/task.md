# タスクリスト：新規タブの「未保存」ラベル表示

## ドキュメント作業
- [x] `spec.md` を最新版に更新
- [x] WIPフォルダを `docs/unsaved_tab_label/` に本番格上げ
- [x] `implementation_plan.md` の作成

## 実装作業（`src/dist/main.js`）
- [x] ① `unsavedTabCounter` 変数の追加
- [x] ② `createNewTab()` の変更（タブ名を `未保存N` / `[未保存N]` に、および `unsavedNumber` を保持）
- [x] ③ `saveTabIfDirty()` の変更（タイムスタンプを保存時刻で生成）
- [x] ④ `triggerManualSave()` の変更（タイムスタンプ生成ロジックの統一）
- [x] ⑤ `formatTabDisplayName()` の変更（未保存ラベルを透過）
- [x] ⑧ `saveSettings()` の変更（保存モード切り替え処理の修正）

## コミット・検証
- [x] 実装内容のコミット＆プッシュ（ユーザー確認後）
- [ ] 動作確認（手動テスト14項目）
  - [ ] 1. 起動直後: タブ名が `未保存1` で表示されること
  - [ ] 2. `+` ボタンで新規タブ追加: `未保存2`、`未保存3` と連番が増えること
  - [ ] 3. 文字を入力して3秒待つ（自動保存）: タブ名が `yyyy/mm/dd hh:mm:ss`（保存実行時の現在時刻）に切り替わること
  - [ ] 4. `Ctrl+S` で手動保存（自動保存モード）: 即座に保存され、タブ名がタイムスタンプに切り替わること
  - [ ] 5. 手動保存モードに切り替えて新規タブ: タブ名が `[未保存1]` で表示されること
  - [ ] 6. 手動保存モードで `Ctrl+S`: タブ名が `[yyyy/mm/dd hh:mm:ss]` に切り替わり、ファイルが作成されること
  - [ ] 7. アプリ再起動後: 連番が `未保存1` からリセットされていること
  - [ ] 8. `未保存1`（内容なし）のまま新規タブを開く: `未保存1` は残り、`未保存2` が開くこと
  - [ ] 9. `未保存1`（内容なし）タブを閉じる: ディスク操作なしでタブが閉じること
  - [ ] 10. 既存ファイルを開く: タブ名が従来通り正常表示されること
  - [ ] 11. `未保存1` (空) の状態で設定から手動モードに変更: タブ名が `[未保存1]` になり、ファイルは作成されないこと
  - [ ] 12. `[未保存1]` (空) の状態で設定から自動モードに変更: タブ名が `未保存1` になり、ファイルは作成されないこと
  - [ ] 13. `[未保存1]` (文字入力あり) で設定から自動モードに変更: 自動で保存が走り、ファイルが作成され、タイムスタンプ名になること
  - [ ] 14. 自動モードで文字入力・保存された状態で手動モードに変更: ファイルは削除されず、タイムスタンプ `[yyyy/mm/dd hh:mm:ss]` を維持すること

# タスクリスト: Ctrl+E 時のアクティブタブ自動フォーカス

- [x] バージョン先行更新および実装計画ドキュメントのコミット & プッシュ (`0.2.33`)
- [x] `src/dist/js/ui/sidebar.js` の `focusSidebarTree` ロジック改修
  - [x] アクティブタブの `filePath` 取得処理の追加
  - [x] 親フォルダ階層の自動展開処理 (`ensureItemVisibleAndExpanded`) の実装
  - [x] アクティブファイルフォーカス & フォールバック（先頭項目選択）処理の実装
- [x] 手動動作検証
  - [x] 階層下のファイルを開いた状態で `Ctrl + E` 押下時の親フォルダ自動展開とフォーカス確認
  - [x] タブ切り替え後の `Ctrl + E` 追従動作確認
  - [x] 未保存・新規タブ時のツリー先頭フォーカス動作確認
　　→ 直前に選択していたファイルにフォーカスが当たっている（※この仕様で良い）

# ウォークスルー: Ctrl+T 新規タブ追加ショートカットの実装

## 完了した作業

1. **`src/dist/js/ui/shortcuts.js` の修正**
   - 既存の `keydown` イベント監視処理に `Ctrl+T` 判定を追加。
   - `tabs.js` から `createNewTab` 関数をインポートし、ショートカット発火時に新規タブを生成するように実装しました。

2. **バージョンと `spec.md` の更新**
   - NoCapEdit のバージョンを `0.1.72` から `0.1.73` にアップデートしました。（`Cargo.toml`, `tauri.conf.json`, `installer.nsi`, `DEVELOPMENT.md`）
   - `spec.md` の 4.3 に `Ctrl+T` によるタブ作成の仕様を追加しました。

3. **アーキテクチャ・ドキュメントの更新**
   - `ARCHITECTURE.md` に「キーボードショートカットのイベント処理はフロントエンドで一元管理する」という実装方針を追記しました。
   - `SHORTCUTS.md` のショートカット一覧に `Ctrl+T` を追加しました。

4. **変更履歴の更新**
   - `history.md` に今回の機能追加（Ver 0.1.73）を追記しました。

## 次のステップ
動作確認後、問題がなければ**「クリーンアップ・マージ」**の指示をお願いします。
（クリーンアップ指示を受けた後、本作業ブランチ `feature/add-tab-shortcut` を `master` へマージし、WIPドキュメントを削除します。）

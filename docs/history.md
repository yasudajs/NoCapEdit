# NoCapEdit 改定履歴 (Changelog)

NoCapEdit のバージョンアップおよび仕様変更の履歴です。
新しいバージョンを上に記載しています。

## 改定履歴一覧

### Ver 0.1.86 | 2026-08-16 | yasudajs
- コードレビュー指摘事項に基づく不具合修正・品質改善
  - **全件置換の特殊文字エスケープ不具合修正**: `findReplace.js` の `replaceAll` において、置換文字列内の `$` パターン（`$1`, `$&` 等）が正規表現パターンとして意図せず展開されるバグを修正
  - **置換ボタンのホバースタイル統一**: `index.html` および `style.css` において、[置換] ボタンの常時薄青色スタイルを解除し、マウスホバー時のみ薄青（アクセント色）に変化するよう統一
  - **input イベント二重発火防止**: `editor.js` の `applyEditorTextWithUndo` において、`execCommand` 成功時の手動 `input` イベント発火を削除し、再描画や自動保存タイマーリセットの二重実行を防止
  - **ショートカット判定の到達不能コード除去**: `shortcuts.js` のズーム拡大条件式から Dead Code（`|| (e.code === 'Semicolon' && e.shiftKey)`）を削除
  - **検索バー入力時のデバウンス導入**: `findReplace.js` のエディタ入力リスナーに 200ms デバウンスを導入し、検索バー表示中の高速入力パフォーマンスを改善
  - **Shift+Tab カーソル計算改善**: `editor.js` の `handleTabKey` において、複数行選択時の行ごとのインデント削除文字数を正確に累積・追跡し、選択範囲ズレを解消
  - **安全性強化とコメント整備**: `editor.js` の `applyWordWrap` に全タブ閉鎖時ガードの JSDoc を追加、および `applyEditorTextWithUndo` に `execCommand` 非推奨 API 使用の背景・注記コメントを追記
  - **ハイライト色・フォーカス色の CSS 変数化**: `style.css` の検索ハイライト色・選択色・フォーカス枠の色を各テーマ定義の CSS 変数（`--search-match-bg`, `--search-current-bg`, `--editor-selection-bg`, `--focus-outline` 等）へ集約
  - **テーマ値のホワイトリスト検証追加**: `help.js` において、URL パラメータから受け取るテーマ値のホワイトリストバリデーション（`['dark', 'soft-dark', 'light']`）を追加

---

### Ver 0.1.85 | 2026-08-16 | yasudajs
- タブスクロールバーの配色改善およびマウスホイール横スクロール機能を実装
  - 各テーマ（Dark / Soft Dark / Light）に合わせてタブスクロールバーのつまみ（thumb）を落ち着いた薄い水色系に変更し、背景と同化する問題を解消
  - スクロールバーつまみにマウスホバーした際の強調ハイライト効果を追加
  - タブ領域上でのマウスホイール回転（上下回転）によるタブ一覧の左右スムーズスクロール処理を実装
- 仕様書 (`spec.md`) を更新

---

### Ver 0.1.84 | 2026-08-16 | yasudajs
- 検索・置換（Find & Replace）機能を実装
  - エディタ右上にテーマ調和のフロート式「検索・置換バー」を追加
  - ショートカットキー `Ctrl + F`（検索）/ `Ctrl + H`（置換）による展開とフォーカス制御
  - テキスト選択状態からの検索語句自動入力
  - 1件置換（`Enter` / [置換] ボタン）およびすべて置換（`Alt + A` / [すべて置換] ボタン）
  - 大文字・小文字の区別切り替え（`[ Aa ]` ボタン / `Alt + C`）
  - `applyEditorTextWithUndo` 経由による Undo/Redo（`Ctrl + Z`）との完全連動（一括置換も1回のUndoで復元）
  - `Esc` によるバー終了およびエディタへの即座なフォーカス復帰
- ヘルプ画面（`F1`）の「テキスト編集」に `Ctrl + F: 検索`, `Ctrl + H: 置換` を追加
- ショートカット一覧 (`SHORTCUTS.md`)、ユーザーガイド (`USER_GUIDE.md`)、仕様書 (`spec.md`) を更新

---

### Ver 0.1.83 | 2026-08-16 | yasudajs
- 行の折り返し切り替え（Word Wrap）機能を実装
  - ショートカットキー（`Alt + Z`）による現在タブの折り返し一時切り替え（タブ個別保持）
  - 設定ドックでのアプリ全体折り返しデフォルト設定および `config.json` 永続化
  - 折り返し無効時の横スクロールバー対応
  - 設定ドック内キーボードナビゲーションに「行の折り返し」を追加
- ヘルプ画面（`F1`）の「表示・ズーム」に `Alt + Z: 行の折り返し切り替え` を追加
- ショートカット一覧 (`SHORTCUTS.md`)、ユーザーガイド (`USER_GUIDE.md`)、仕様書 (`spec.md`) を更新

---

### Ver 0.1.82 | 2026-08-15 | yasudajs
- 設定画面（設定ドック）のキーボード完全操作（アクセシビリティ）を実装
  - 開いた際の [参照...] ボタンへの初期フォーカス自動適用
  - `Tab` / `Shift + Tab` による設定項目間の順次移動（循環ループ対応）
  - 方向キー `↑` / `↓` および `Enter` / `Space` での項目変更
  - `Esc` または `Ctrl + ,` での即座なエディタ復帰（フォーカス・カーソル位置復元）
- フォーカスが当たっている要素の `:focus-visible` スタイル（アウトライン）を強化
- ショートカット一覧 (`SHORTCUTS.md`)、ユーザーガイド (`USER_GUIDE.md`)、仕様書 (`spec.md`) を更新

---

### Ver 0.1.81 | 2026-08-15 | yasudajs
- 設定画面（設定ドック）開閉ショートカットキー（`Ctrl + ,`）を追加
- ズーム・行間リセットショートカットキー（`Ctrl + 0`）を追加（フォント20pt、行間1.5倍に復元）
- ヘルプ画面（`F1`）に `Ctrl + ,` および `Ctrl + 0` を追加
- ショートカット一覧 (`SHORTCUTS.md`)、ユーザーガイド (`USER_GUIDE.md`)、仕様書 (`spec.md`) を更新

---

### Ver 0.1.80 | 2026-08-15 | yasudajs
- タイムスタンプ挿入ショートカットキー（`F5`）を追加（`YYYY/MM/DD HH:mm` 形式、Undo/Redo 対応）
- ヘルプ画面（`F1`）の「テキスト編集」カテゴリに `F5: 現在日時の挿入` を追加
- ショートカット一覧 (`SHORTCUTS.md`)、ユーザーガイド (`USER_GUIDE.md`)、仕様書 (`spec.md`) を更新

---

### Ver 0.1.79 | 2026-08-15 | yasudajs
- ヘルプ画面（`F1`）のショートカット一覧最下部に余白を追加し、最終行の視認性を改善

---

### Ver 0.1.78 | 2026-08-15 | yasudajs
- 行操作（上下移動、上下複製、削除）およびインデント操作（`Tab` / `Shift + Tab`）において、ブラウザ標準の Undo（`Ctrl + Z`）/ Redo（`Ctrl + Y`）と完全連動するように改修
- 通常の文字入力とショートカット操作の編集履歴を一本化し、シームレスに元に戻せるよう改善
- 仕様書 (`spec.md`) を更新

---

### Ver 0.1.77 | 2026-08-15 | yasudajs
- 行の上下移動ショートカットキー (`Alt + ↑` / `Alt + ↓`) を追加（単一行・複数行選択に対応）
- 行の上下複製ショートカットキー (`Alt + Shift + ↑` / `Alt + Shift + ↓`) を追加（単一行・複数行選択に対応）
- 行の削除ショートカットキー (`Ctrl + Shift + K`) を追加（単一行・複数行選択に対応）
- ヘルプ画面（`F1`）に「テキスト編集」カテゴリを新設し、行操作ショートカットを追加
- ショートカット一覧 (`SHORTCUTS.md`)、ユーザーガイド (`USER_GUIDE.md`)、仕様書 (`spec.md`) を更新

---

### Ver 0.1.76 | 2026-08-15 | yasudajs
- アクティブなタブを閉じるショートカットキー (`Ctrl+W`) を追加（最後のタブを閉じた場合は自動で新規空タブを開く）
- アプリケーションを安全に保存して終了するショートカットキー (`Ctrl+Q`) を追加
- ヘルプ画面（`F1`）のショートカット一覧に `Ctrl+W` と `Ctrl+Q` を追加
- ヘルプ画面およびショートカット一覧の表記を「エクスプローラーを開く」に微調整
- ショートカット一覧 (`SHORTCUTS.md`)、ユーザーガイド (`USER_GUIDE.md`)、仕様書 (`spec.md`) を更新

---

### Ver 0.1.75 | 2026-08-15 | yasudajs
- `F1` キーで開くことができる「ヘルプ機能（ショートカット一覧）」を追加
- ヘルプ画面は独立したウィンドウで表示され、多言語対応（i18n）を適用

---


### Ver 0.1.74 | 2026-08-14 | yasudajs
- `Ctrl+E` ショートカットで、保存先（ホームフォルダ）をOS標準のエクスプローラーで開く機能を追加
- ショートカット一覧 (`SHORTCUTS.md`) および仕様書 (`spec.md`) を更新

---


### Ver 0.1.73 | 2026-08-14 | yasudajs
- 新規タブを追加するためのキーボードショートカット (`Ctrl+T`) を追加
- ショートカットキーの実装方針（フロントエンドでの一元管理）を `ARCHITECTURE.md` に追記
- ショートカット一覧 (`SHORTCUTS.md`) を更新

---

### Ver 0.1.72 | 2026-08-13 | yasudajs
- タブを閉じた際の挙動を改善。フォーカス中のタブを閉じた際、常に一番左側のタブへフォーカスが移動するのではなく、閉じたタブの直ぐ隣のタブ（右隣、存在しなければ左隣）へフォーカスが移動するように修正
- リファクタリングによるファイル数増加に伴い、内部設計・役割分担を定義する `ARCHITECTURE.md` を新設し、関連ドキュメント（`spec.md`, `DEVELOPMENT.md`）を整理

---


### Ver 0.1.71 | 2026-08-13 | yasudajs
- 手動保存モードの初回保存で、JS側でのパス文字列結合（`getPathSeparator()` + 文字列連結）を廃止し、自動保存と同様にRust側の `create_and_save_file` コマンド（`PathBuf::join()` によるパス結合）を使用するように統一（リファクタリングレビュー指摘事項 7 の改善）
- 不要になった `getPathSeparator()` 関数を `helpers.js` から削除

---

### Ver 0.1.70 | 2026-08-13 | yasudajs
- `i18n.js` のESモジュール化に伴い、UIモジュール（`tabs.js`, `editor.js`）で翻訳関数 `t` が未定義となりアプリの初期化が停止していたバグを修正

---

### Ver 0.1.69 | 2026-08-13 | yasudajs
- 多言語対応に向けた準備として導入された `i18n.js` について、`window.t` 等を用いたグローバル関数登録を廃止し、ESモジュールの `export/import` を利用するモダンな設計にリファクタリング（リファクタリングレビュー指摘事項 10）

---

### Ver 0.1.68 | 2026-08-13 | yasudajs
- 将来的な他モジュールからのデータ参照を見据え、`commands.rs` 内の `FileInfo` 構造体のフィールドを `pub` に変更（リファクタリングレビュー指摘事項 9）

---

### Ver 0.1.67 | 2026-08-13 | yasudajs
- `updater.js` が直接DOMを操作していた処理を、新規作成した UI 層の `updaterUI.js` に分離し、関心の分離（SoC）を徹底（リファクタリングレビュー指摘事項 8）

---

### Ver 0.1.66 | 2026-08-13 | yasudajs
- 将来的なマルチプラットフォーム対応に向け、`fileSystem.js` 内の手動保存時ファイルパス結合でハードコードされていた `\\` を、OSに応じて切り替わるように修正（リファクタリングレビュー指摘事項 7）

---

### Ver 0.1.65 | 2026-08-13 | yasudajs
- 単一インスタンス動作時の別プロセスへのファイルパス送信（TCP通信）において、書き込みエラーを無視して常に成功を返していた問題を修正（リファクタリングレビュー指摘事項 6）

---

### Ver 0.1.64 | 2026-08-13 | yasudajs
- `settings.js` の `saveSettings()` 内にハードコードされていた「保存モード変更時の副作用処理」を別関数 `handleSaveModeSwitch()` に抽出し、処理の凝集度と可読性を向上（リファクタリングレビュー指摘事項 5）

---

### Ver 0.1.63 | 2026-08-13 | yasudajs
- `state.js` のDOM要素キャッシュにおいて、モジュールロード時の無意味な `document.getElementById` 呼び出しをやめ、明示的に `null` で初期化するよう修正し、コードの意図を明確化（リファクタリングレビュー指摘事項 4）

---

### Ver 0.1.62 | 2026-08-13 | yasudajs
- 定数 `AUTO_FILE_REGEX` が `helpers.js` と `state.js` で重複定義されていた問題を解消し、不要な `state.js` 側の定義を削除（リファクタリングレビュー指摘事項 3）

---

### Ver 0.1.61 | 2026-08-13 | yasudajs
- `commands.rs` と `cli.rs` 間で重複していたコマンドライン引数（起動ファイルパス）の取得ロジックを共通化（DRY化）し、相対パス解決漏れのリスクを解消（リファクタリングレビュー指摘事項 2）

---

### Ver 0.1.60 | 2026-08-13 | yasudajs
- `settings.rs` の `env!("USERPROFILE")` によるコンパイル時環境変数の埋め込み問題を修正し、実行時に `USERPROFILE` または `HOME` を取得するよう変更（リファクタリングレビュー指摘事項 1）

---

### Ver 0.1.59 | 2026-08-10 | yasudajs
- ダイアログ関連のUI操作を `fileSystem.js` から `ui/dialogs.js` に抽出し、コアロジック層とUI層の依存関係を分離（リファクタリング ステップ 5.1）

---

### Ver 0.1.58 | 2026-08-10 | yasudajs
- グローバルなショートカットキー監視処理を `main.js` から `shortcuts.js` に分離し、メインプロセスの初期化コードを整理（リファクタリング ステップ 4.1）

---

### Ver 0.1.57 | 2026-08-10 | yasudajs
- テーマおよびフォント適用ロジックを `settings.js` から `theme.js` に抽出し、設定画面UI制御の責務と分離（リファクタリング ステップ 3.3）

---

### Ver 0.1.56 | 2026-08-10 | yasudajs
- アプリ全体の設定保存ロジックを `settings.js` から `settingsManager.js` に抽出し、設定管理の依存基盤を整理（リファクタリング ステップ 3.2）

---

### Ver 0.1.55 | 2026-08-10 | yasudajs
- アップデートチェックのロジックを `settings.js` から `updater.js` に抽出し、設定画面UI制御の責務と分離（リファクタリング ステップ 3.1）

---

### Ver 0.1.54 | 2026-08-08 | yasudajs
- `fileSystem.js` 内のタブID生成ロジックを `helpers.js` の `generateTabId` に置き換え、ハードコードされた冗長なコードを排除（リファクタリング ステップ 2.2）

---

### Ver 0.1.53 | 2026-08-08 | yasudajs
- JSフロントエンドのタイムスタンプ生成ロジック（`YYYYMMDD_HHmmss`）を `helpers.js` の `generateTimestamp` 関数に共通化し、コードの重複を排除（リファクタリング ステップ 2.1）

---

### Ver 0.1.52 | 2026-08-08 | yasudajs
- Rustバックエンドの初期化処理（コマンドライン引数解析、初期テーマ適用）をそれぞれ `cli.rs` と `theme.rs` に抽出しモジュール化（リファクタリング ステップ 1.4）

---

### Ver 0.1.51 | 2026-08-08 | yasudajs
- RustバックエンドのTauriコマンドハンドラ群（`get_settings`, `save_text_file` など多数）を `main.rs` から `commands.rs` に抽出しモジュール化（リファクタリング ステップ 1.3）

---

### Ver 0.1.50 | 2026-08-08 | yasudajs
- Rustバックエンドの単一インスタンス制御ロジック（ポート監視、プロセス間通信）を `main.rs` から `instance.rs` に抽出しモジュール化（リファクタリング ステップ 1.2）

---

### Ver 0.1.49 | 2026-08-08 | yasudajs
- Rustバックエンドの設定管理ロジック（`AppSettings`等）を `main.rs` から `settings.rs` に抽出しモジュール化（リファクタリング ステップ 1.1）

---

### Ver 0.1.48 | 2026-08-05 | yasudajs
- Rustバックエンド(`main.rs`)から返却されるエラーメッセージをハードコードからキー（`fs.error.*`）に置換し、JS側で受け取って翻訳適用する仕組みを実装（Step 4.1）

---

### Ver 0.1.47 | 2026-08-05 | yasudajs
- `index.html` 内の静的テキスト（設定ダイアログ、ツールチップなど）を多言語化対応。`data-i18n` 属性による一括翻訳適用処理を実装（Step 3.2）

---

### Ver 0.1.46 | 2026-08-05 | yasudajs
- `main.js` 内の初期化・終了時エラーやステータス通知メッセージを `i18n.js` に抽出し、多言語化対応（Step 3.1）

---

### Ver 0.1.45 | 2026-08-05 | yasudajs
- `tauri.js` 内に存在した循環参照の原因となる不要なUI更新依存とコメントアウト行を完全に削除し、同じ失敗（循環参照によるクラッシュ）を繰り返さないための警告コメントをソースコード上に追記（Step 2.2 対応）

---

### Ver 0.1.44 | 2026-08-05 | yasudajs
- `fileSystem.js` 内のハードコードされた日本語文字列（保存・読み込み時のエラーダイアログ、成功通知など）を `i18n.js` に抽出し、多言語化関数 `window.t()` に置き換え（Step 2.1）

---

### Ver 0.1.43 | 2026-08-04 | yasudajs
- `editor.js` および `index.html` にハードコードされていたテキスト（ステータスバーのメトリクス、プレースホルダー）を `i18n.js` に抽出・置換（Step 1.3）
  - 多言語化関数 `window.t()` にテンプレート変数置換機能を追加し、言語ごとの語順の違いを吸収できるように拡張

---

### Ver 0.1.42 | 2026-08-04 | yasudajs
- `tabs.js` 内のハードコードされた日本語文字列（ステータス表示やエラー等）を `i18n.js` に抽出し、多言語化関数 `window.t()` に置き換え（Step 1.2）

---

### Ver 0.1.41 | 2026-08-02 | yasudajs
- `settings.js` 内のハードコードされた日本語文字列を `i18n.js` に抽出し、多言語化関数 `window.t()` に置き換え（Step 1.1）
- 多言語対応（`src/dist/i18n.js`）のキー表現をスネークケースからドット区切り階層構造オブジェクト（`category.subcategory.key`）に変更
- `window.t(key)` 関数をドット区切りキーの階層展開・探索処理に対応させ、未登録キーのフォールバック動作を整備

---

### Ver 0.1.40 | 2026-07-25 | yasudajs
- 設定画面で保存モードを変更した際に、`main.js` のイベントリスナー内で `saveSettings` 関数のインポートが欠落していたため、ReferenceError が発生してUIが更新されず設定も保存されない不具合（Ver 0.1.38 デグレード）を修正
- 初期化処理において起動条件に関わらずイベントリスナーが一括登録されるよう制御フローを改善

---

### Ver 0.1.39 | 2026-07-17 | yasudajs
- アプリ起動時の自動アップデートチェック機能において、起動中のアプリのバージョン系統（メジャー・マイナー）に対応する最新バージョンのみを検知する系統分離制御を導入
- v0.1系とv0.2系が並存する運用において、他系統のアップデート通知を受け取らないようにGitHub Releasesの一覧から適切なタグを抽出するロジックに修正

---

### Ver 0.1.38 | 2026-07-16 | yasudajs
- フロントエンドJavaScriptのコードを単一ファイルからESモジュール分割構造へリファクタリング
- 多言語対応（i18n.js）のインフラ導入とindex.htmlのスクリプト読み込みのモジュール化
- v0.2系からタブ切り替えの自動スクロールおよび Ctrl + Tab / Ctrl + Shift + Tab ナビゲーションを逆移植

---

### Ver 0.1.37 | 2026-07-15 | yasudajs
- リリースビルドにおいて、Webブラウザ標準の右クリックコンテキストメニュー（印刷、再読み込み、保存など）を無効化し、アプリの操作性とデザインの統一感を向上
- 開発デバッグ時の利便性を損なわないよう、デバッグビルド時（debug_assertions有効時）は標準コンテキストメニューを有効のまま維持する制御を導入

---

### Ver 0.1.36 | 2026-07-13 | yasudajs
- 不要なブラウザ標準ショートカット（Ctrl + R, F5 によるページ再読み込み）を無効化し、誤操作によるデータ消失を防ぐ安全対策を強化
- 見栄えの悪い標準印刷ダイアログを起動させないよう、Ctrl + P ショートカットを無効化
- README.mdの各種機能説明を専門用語を避けた平易な表現にリライトし、保存先説明から不要なシステムパス情報を削除

---

### Ver 0.1.35 | 2026-07-12 | yasudajs
- 新規空タブでCtrl+Sを実行した際に、実際には保存（ファイル作成）がスキップされているにもかかわらずステータスバーが「作成」表示になってしまう不具合を修正
- タブを閉じた際にアクティブになったタブの保存状態がステータスバーに同期されず、閉じたファイルの状態が残る不具合を修正
- 設定画面を右ドック化した際に、アップデート通知のバージョン表示がはみ出して改行されてしまうレイアウト崩れを防ぐため、通知テキスト内に改行を挿入

---

### Ver 0.1.34 | 2026-07-12 | yasudajs
- 新規タブ（ファイル未作成）状態の表示名を自動保存モード時は「未保存N」、手動保存モード時は「[未保存N]」に変更し、連番での識別に改善（連番はアプリ再起動時にリセット）
- ファイル名のタイムスタンプ（`yyyymmdd_hhmmss.nctx`）の基準を、タブ作成時刻ではなく最初の保存（ディスク書き込み）が実行された時刻に変更
- 設定画面で自動保存モードと手動保存モードを動的に切り替えた際、未作成の「未保存N」/「[未保存N]」タブ名が、現在のモードや元の連番を維持したまま正しく切り替わるよう修正

---

### Ver 0.1.33 | 2026-07-11 | yasudajs
- 新規タブ作成（遅延作成待ち状態）時におけるステータスバーの初期表示を改善（自動保存時: 「保存準備完了」、手動保存時: 「[手動保存モード] ※Ctrl+Sで保存できます」）
- ディスク上にファイルが実在する状態において、ステータスが「編集中」や「保存済み」等に変化してもファイル名が消えないように「ファイル名 - 状態」の表示形式に統一
- 手動保存モード時における、ファイル実在状態の接頭辞を「[手動保存:Ctrl+S]」に短縮化
- タブを切り替えた際、そのタブの直前のステータス状態（保存済み / 編集中 / 未作成の準備完了等）を自動判定してステータスバー表示を正確に復元する機能を実装

---

### Ver 0.1.32 | 2026-07-11 | yasudajs
- ソフトダークテーマを追加（テーマ選択肢がダーク/ソフトダーク/ライトの3択に拡張）
- テーマ切り替えUIをトグルボタンからドロップダウンに変更し、他の設定項目と統一
- テーマ表示名をカタカナ（ダーク/ソフトダーク/ライト）に統一

---

### Ver 0.1.31 | 2026-07-11 | yasudajs
- 設定保存処理（save_settings）をDRY化（JS側のラッパー共通化とRust側の構造体引数化）し、パラメータ追加時の追随漏れを防止する構造にリファクタリング

---

### Ver 0.1.30 | 2026-07-11 | yasudajs
- 新規タブ作成時および起動時のファイル実体の自動生成を初回保存時まで遅延するよう変更
- 新規タブで入力後に全削除して閉じた際、ディスクに不要な空ファイルが作成されるのを防止するガードを追加
- 起動時のウィンドウ描画のガタつきを防止するため、初期化完了後に画面を表示する制御を導入
- Windowsのインストールアプリ一覧に表示されるバージョンが古いままになるインストーラーバグを修正
- テーマ切り替え時に設定が保存されず、再起動すると元のテーマに戻る不具合を修正

---

### Ver 0.1.29 | 2026-07-11 | yasudajs
- 設定画面閉鎖時のエディタフォーカスおよび状態復元機能を実装
- 新規ウィンドウ起動・新規タブ作成時のエディタ自動フォーカスを実装
- タブ切り替え時のカーソル位置・スクロール位置の個別保持と復元を実装
- 新規ファイル作成時のプレースホルダーを「入力準備完了」へ更新

---

### Ver 0.1.28 | 2026-07-10 | yasudajs
- 設定画面の右ドック化および設定変更の即時反映機能を実装

---

### Ver 0.1.27 | 2026-07-10 | yasudajs
- 文字数カウント方法（改行を含む/除く）を選択できる設定項目を追加

---

### Ver 0.1.26 | 2026-07-10 | yasudajs
- 範囲選択時の文字数カウントおよびステータスバー表示機能を追加

---

### Ver 0.1.25 | 2026-07-09 | yasudajs
- 設定画面から自動保存モードと手動保存モードを切り替える機能を追加

---

### Ver 0.1.24 | 2026-07-09 | yasudajs
- 配布用パッケージ（インストーラー）の画面を日本語化

---

### Ver 0.1.23 | 2026-07-09 | yasudajs
- タブ表示名を自動生成ファイルは日時、その他は拡張子非表示にするよう変更

---

### Ver 0.1.22 | 2026-07-09 | yasudajs
- 設定画面の各入力欄のインデント（左余白）を揃えるようレイアウトを調整

---

### Ver 0.1.21 | 2026-07-09 | yasudajs
- タブとエディタの境界線を削除し、タブと追加ボタンの間隔を調整

---

### Ver 0.1.20 | 2026-07-09 | yasudajs
- ローカルバージョンが最新より新しい場合にアップデート通知が出る不具合を修正

---

### Ver 0.1.19 | 2026-07-09 | yasudajs
- 文字数カウントから改行を除外するよう仕様変更

---

### Ver 0.1.18 | 2026-07-09 | yasudajs
- 行間設定のステータスバー表示を倍率表記に変更

---

### Ver 0.1.17 | 2026-07-09 | yasudajs
- アプリ起動時の自動アップデートチェックおよび通知機能を追加

---

### Ver 0.1.16 | 2026-07-09 | yasudajs
- タブ追加ボタンの配置変更およびデザイン修正

---

### Ver 0.1.15 | 2026-07-09 | yasudajs
- 設定値・定数の一元管理（ハイブリッドアプローチ）のリファクタリング
  - Rust（基本定数）、CSS変数（UIデザイン）、JS（フロントエンド固有）へと定数管理の責務を分離し、ハードコード・二重管理を解消

---

### Ver 0.1.14 | 2026-07-09 | yasudajs
- タブ入力挙動の制御およびインデント操作機能の追加
  - Tabキーによるタブ入力、Shift+Tabでのインデント削除、複数行選択への適用、および設定画面からの挙動選択・永続化

---

### Ver 0.1.13 | 2026-07-09 | yasudajs
- 行間高さ調整機能の追加
  - Ctrl + Shift + マウスホイール または +/- による行間変更とステータスバー表示

---

### Ver 0.1.12 | 2026-07-08 | yasudajs
- 設定画面をモーダル化し、歯車アイコンで表示。フォント・テーマ設定を統合

---

### Ver 0.1.11 | 2026-07-06 | yasudajs
- Ctrl+Sによる手動上書き保存機能の追加

---

### Ver 0.1.10 | 2026-07-06 | yasudajs
- バージョン番号の一元管理仕様へのリファクタリング

---

### Ver 0.1.9 | 2026-07-06 | yasudajs
- フォント選択機能の遅延ロード化（起動高速化）への変更

---

### Ver 0.1.8 | 2026-07-05 | yasudajs
- ステータスバー右下のファイル名表示を削除し、フォントサイズ表示を追加

---

### Ver 0.1.7 | 2026-07-05 | yasudajs
- アプリアイコンの刷新

---

### Ver 0.1.6 | 2026-07-05 | yasudajs
- 空ファイルが削除されない不具合の修正
  - 自動作成された一時ファイルが一度入力されその後空にされた場合も、終了時に正しく削除されるよう修正

---

### Ver 0.1.5 | 2026-07-05 | yasudajs
- 単一ウィンドウ動作（シングルインスタンス）仕様の追加
  - 多重起動防止、別プロセス起動時の引数ファイル受け渡しとタブ追加・ウィンドウフォーカス

---

### Ver 0.1.4 | 2026-07-04 | yasudajs
- タイトルバーへのバージョン表示機能の実装
  - アプリ名の隣にバージョン番号を `NoCapEdit [ Ver a.b.c ]` 形式で表示（Tauriバックエンドから自動取得・DRY化）

---

### Ver 0.1.3 | 2026-07-04 | yasudajs
- フォントファミリー選択機能の実装
  - システムフォントの分類検出、セレクトボックスによる切り替え、設定の保存

---

### Ver 0.1.2 | 2026-07-04 | yasudajs
- テーマ切り替え機能およびフォント拡大縮小機能の実装
  - ライト・ダークモードのUI切り替え、設定の保存、およびWindowsタイトルバーの黒化
  - Ctrl+ホイール、Ctrl+ +/- によるズーム、および設定の保存

---

### Ver 0.1.1 | 2026-07-04 | yasudajs
- 独自拡張子 `.nctx` の採用、OS関連付け、ダブルクリック起動の追加
  - 保存ダイアログのフィルター対応

---

### Ver 0.1.0 | 2026-07-04 | yasudajs
- MVP初期仕様確定、アプリアイコン透過等の追記
- ウィンドウの透過アプリアイコン適用

---

### Ver 0.0.0 | 2026-07-04 | yasudajs
- 初期開発フェーズ
  - Phase 1: 基本基盤 — Tauriのセットアップ、基本UI、ホームフォルダ設定の永続化
  - Phase 2: 自動生成 & 保存 — 起動時のファイル自動生成、連番衝突回避、安全な上書き保存
  - Phase 3: 編集 & 表示 — タブUI、テキスト編集エリア、ステータスバー表示
  - Phase 4: 自動クリーンアップ & 競合防止 — Debounce自動保存、タブ切替保存、タブ閉じる・アプリ終了時の自動保存およびクリーンアップ、競合防止の排他制御

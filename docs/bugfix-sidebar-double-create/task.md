# タスクリスト: Sidebar Double Creation Bug Fix (Step 5-1-3)

- [x] `sidebar.js` の `itemDiv` に対する `keydown` リスナーに `e.stopPropagation()` を追加
- [x] 名前変更および新規作成の `input` リスナーで `Escape` / `Enter` 時の `e.stopPropagation()` / `e.preventDefault()` 追加
- [x] 新規ファイル作成確定時にツリーの選択状態（非アクティブ選択）を更新し、古い選択をクリアする処理を追加
- [ ] 動作確認（手動検証）
  - [x] ディレクトリ `aaa` を作成し、フォーカスがある状態でさらに `Ctrl+D` を押下した際、1つだけ「新しいフォルダ」が編集状態で作成されることを確認
  - [x] 同様にフォーカスがある状態で `Ctrl+N` を押下した際、1つだけ「名称未設定.nctx」が編集状態で作成されることを確認
  - [x] 矢印キー（上下）での項目移動が動作することを確認
  - [x] 矢印キー（左右）での展開・折りたたみが動作することを確認
  - [x] `Enter` でファイルオープン・フォルダ展開・トグルが動作することを確認
  - [x] `Esc` でエディタにフォーカスが戻ることを確認
  - [x] [F2名前変更キャンセル] `F2` で名前変更が開始され、`Esc` でキャンセルした際、フォーカスがツリー項目に留まり、エディタにフォーカスが移らないことを確認 (不具合1)
  - [x] [新規ファイル選択] `Ctrl+N` で新規ファイルを作成し、`Enter` で確定した際、新しく作成されたファイルがツリー側で非アクティブ選択（グレー）状態になり、古い選択がクリアされることを確認 (不具合2)
  - [x] `Ctrl+C`, `Ctrl+X`, `Ctrl+V` が正常に動作することを確認
- [ ] `docs/history.md` に変更履歴を追記
- [ ] コミット＆プッシュ

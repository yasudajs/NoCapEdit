# タスクリスト - Phase 5.3: ツリー更新時のスクロール・選択状態の完全保持

- [x] バージョン更新・環境準備
  - [x] 作業用ブランチ `feature/phase-5-3` の作成
  - [x] バージョン管理ファイルの更新 (0.2.12 -> 0.2.13)
  - [x] `docs/spec.md` の更新
- [x] `sidebar.js` のリファクタリング (差分更新の実装)
  - [x] 単一 tree-item 生成処理のヘルパー関数化 (`createTreeItemElement(file)`)
  - [x] `renderFileTree` を差分更新アルゴリズムに置き換え
    - [x] 既存DOM要素 (ペア) のスキャン
    - [x] 不要DOM要素の削除 (`remove()`)
    - [x] 新規/既存DOM要素 of 順序整列 (`container.appendChild`) とテキスト更新
    - [x] 既存フォーカスおよび選択状態の復元ロジック
    - [x] フォルダ展開の再帰処理 (`loadDirectory` の呼び出し)
  - [x] フルリロードを行うパスの整理 (ルートパス変更時)
- [x] `style.css` の調整 (ライトモード選択カラー)
  - [x] `body.light-theme .tree-item.selected` に `#CCE8FF` の背景色と `var(--text-primary)` の文字色を設定
- [/] 動作確認・検証
  - [x] アプリケーションのビルドと起動 (`cargo tauri dev`)
  - [x] スクロール位置の維持の検証 (手動/自動保存、外部変更時)
  - [x] フォーカスと選択状態の維持の検証
  - [x] 展開状態の維持の検証
  - [x] 新規作成・名前変更の正常動作の検証
    - [x] Ctrl+N / Ctrl＋D の操作をしたとき、現在選択中のアイテムを基準に作成されるよう修正済
    - [x] 上記の動作をしたとき、ESCしたとき、直前にフォーカスしていた場所にフォーカスが戻るよう修正済
  - [x] ライトモードの選択ハイライト色の検証

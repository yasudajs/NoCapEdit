# タスクリスト

- [x] バックエンド (Rust) でのファイル操作 API 実装
  - [x] `Cargo.toml` に `trash` クレートを追加
  - [x] `create_file_or_dir` コマンドの実装（セキュリティチェック、連番衝突回避）
  - [x] `rename_file_or_dir` コマンドの実装（セキュリティチェック、既存重複チェック）
  - [x] `trash_file_or_dir` コマンドの実装（セキュリティチェック、ゴミ箱移動）
  - [x] Tauri のハンドラへの登録
- [x] フロントエンド (HTML / CSS / JS) での UI & 制御の実装
  - [x] `index.html` にカスタムコンテキストメニューの DOM 追加
  - [x] `style.css` にコンテキストメニューとインライン編集用入力欄のスタイル追加
  - [x] `main.js` にコンテキストメニュー制御ロジック追加（右クリック判定、出し分け、非表示制御）
  - [x] `main.js` にインライン編集ロジック追加（ファイル/フォルダ新規作成時のインプット表示、確定・キャンセル処理）
  - [x] `main.js` に名前変更ロジック追加（インプット表示、確定・キャンセル処理）
  - [x] `main.js` に削除処理と確認ダイアログ追加
  - [x] `main.js` にエディタタブとの連動ロジック追加（削除時タブクローズ、名前変更時タブパス・表示名更新）
- [ ] 動作検証と確認
  - [ ] 1. コンテキストメニューの表示確認
    - [ ] ファイル右クリック時、正しい項目（名前変更、削除）のみが表示されること
    - [ ] フォルダ右クリック時、正しい項目（新規ファイル、新規フォルダ、名前変更、削除）が表示されること
    - [ ] サイドバー背景右クリック時、正しい項目（新規ファイル、新規フォルダ）のみが表示されること
    - [ ] メニュー外のクリックや `Esc` キーでメニューが閉じること
    - [ ] 各テーマ（Dark / Soft Dark / Light）でコンテキストメニューのデザインが崩れず追従すること
  - [ ] 2. 新規作成（インライン編集）の動作確認
    - [ ] 空入力（Enter または blur）で、デフォルト名（ファイル: `名称未設定.nctx` / フォルダ: `新しいフォルダ`）で作成されること
    - [ ] すでにデフォルト名が存在する場合、連番（例: `名称未設定_01.nctx` / `新しいフォルダ_01`）で衝突回避されること
    - [ ] 拡張子なしの入力（例: `memo`）で、自動的に `.nctx` が補完（`memo.nctx`）されて作成されること
    - [ ] 新規ファイル作成の完了後、自動的にエディタの新規タブとして開かれること
    - [ ] `Esc` キーで新規作成が正しくキャンセルされること
  - [ ] 3. 名前変更（インライン編集）の動作確認
    - [ ] ファイル・フォルダの名前をインラインで正常に変更できること
    - [ ] 名前変更時、入力欄フォーカス時に拡張子以外のファイル名部分のみが初期選択されていること
    - [ ] すでに存在する名前へ変更しようとした際、エラーメッセージ（ダイアログ）が表示され変更されないこと
    - [ ] エディタで開いているファイルの名前を変更した際、タブの表示名が新しいファイル名に追従すること
    - [ ] エディタで開いているファイルの名前を変更した際、内部のファイルパスが更新され、その後の保存が新パスに行われること
    - [ ] `Esc` キーまたは元の名前と同名での確定時に名前変更が正しくキャンセルされること
  - [ ] 4. 通常削除の動作確認
    - [ ] 削除選択時に確認ダイアログ（Tauriダイアログ）が表示されること
    - [ ] ダイアログで「いいえ」を選択した際、削除がキャンセルされること
    - [ ] ダイアログで「はい」を選択した際、ファイル・フォルダが実際にOSのゴミ箱に移動すること
    - [ ] エディタで開いているファイルを削除した際、エディタの対象タブが自動で閉じられること

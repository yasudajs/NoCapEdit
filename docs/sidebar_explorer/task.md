# タスクリスト

- [ ] バックエンド (Rust) でのファイル操作 API 実装
  - [ ] `Cargo.toml` に `trash` クレートを追加
  - [ ] `create_file_or_dir` コマンドの実装（セキュリティチェック、連番衝突回避）
  - [ ] `rename_file_or_dir` コマンドの実装（セキュリティチェック、既存重複チェック）
  - [ ] `trash_file_or_dir` コマンドの実装（セキュリティチェック、ゴミ箱移動）
  - [ ] Tauri のハンドラへの登録
- [ ] フロントエンド (HTML / CSS / JS) での UI & 制御の実装
  - [ ] `index.html` にカスタムコンテキストメニューの DOM 追加
  - [ ] `style.css` にコンテキストメニューとインライン編集用入力欄のスタイル追加
  - [ ] `main.js` にコンテキストメニュー制御ロジック追加（右クリック判定、出し分け、非表示制御）
  - [ ] `main.js` にインライン編集ロジック追加（ファイル/フォルダ新規作成時のインプット表示、確定・キャンセル処理）
  - [ ] `main.js` に名前変更ロジック追加（インプット表示、確定・キャンセル処理）
  - [ ] `main.js` に削除処理と確認ダイアログ追加
  - [ ] `main.js` にエディタタブとの連動ロジック追加（削除時タブクローズ、名前変更時タブパス・表示名更新）
- [ ] 動作検証と確認
  - [ ] コンテキストメニューの表示確認（テーマ追従）
  - [ ] 新規作成（ファイル/フォルダ）の動作確認（空入力デフォルト名、連番衝突回避、許可拡張子補完、自動タブオープン）
  - [ ] 名前変更の動作確認（重複エラー、タブ表示名・パスの追従）
  - [ ] 削除の動作確認（ダイアログ、ゴミ箱、タブクローズ）

# タスクリスト: Phase 4 高度なファイル操作

- [x] バックエンド (Rust) の実装
  - [x] `delete_file_or_dir_permanently` コマンドの実装
  - [x] `move_file_or_dir` コマンドの実装
  - [x] コマンドの登録 (`tauri::generate_handler!`)
  - [x] Rust側のビルド確認
- [x] フロントエンド (CSS) の実装
  - [x] 選択状態（アクティブ/非アクティブ）のCSS追加
  - [x] ドラッグホバー時のCSS追加
- [x] フロントエンド (JS) の実装
  - [x] ツリー上の項目選択（クリックハイライト）管理
  - [x] エディタ等へのフォーカス移動時の非アクティブ選択への切り替え
  - [x] キーボード監視と `Delete`/`Shift + Delete` イベントの処理
  - [x] 完全削除 `deleteItemPermanentlyInTree` 関数の実装
  - [x] ドラッグ＆ドロップの実装（`dragstart`, `dragover`, `dragenter`, `dragleave`, `drop`, `dragend`）
  - [x] ステータスバー（`statusText`）へのエラー表示および次の操作までの維持
  - [x] ツリー余白部分へのドロップイベント追加とルート（appState.homeFolder）への移動処理
  - [x] D&D移動時に該当するタブのパス情報を事前に新パスへ書き換えてタブと編集状態を維持する処理
- [x] 動作確認・検証
  - [x] 選択状態（ハイライト）の検証
    - [x] ツリー上の項目（ファイル・フォルダ）をクリックしてアクティブ色（青）でハイライトされること
    - [x] エディタ領域等をクリック（または文字入力）したとき、ツリーのハイライトが非アクティブ色（グレー）に変わること
      → 対策完了：ライトテーマの非アクティブ時の背景色を濃いグレー（#d0d0d0）に調整。再テスト待ち。
      → 再テストOK
    - [x] ツリー外の余白（背景）をクリックしたとき、ツリーの選択状態が解除されること
  - [x] 完全削除（Shift + Delete）の検証
    - [x] ツリー項目を選択して `Shift + Delete` を押した際、「完全に削除しますか？」の警告ダイアログ（モーダル）が表示されること
      → 対策完了：ツリー項目にtabindex="0"を付与し、クリック時にフォーカスを当てることでキーイベントがエディタに奪われる問題を解消。再テスト待ち。
      → 再テストOK
    - [x] ダイアログで「はい」を選択した際、対象のファイル/フォルダがOSのごみ箱に入らず、物理的に直接削除されること
    - [x] 開いているファイルを完全に削除した際、エディタ上の該当タブが自動的に閉じられること
    - [x] 単に `Delete` キーを押した場合は、既存の通常削除（ごみ箱への移動確認ダイアログ）が表示されること
      → 対策完了：フォーカス問題の解消により、Deleteキーでの通常削除も連動して動作するよう修正。再テスト待ち。
      → 再テストOK
  - [x] ドラッグ＆ドロップ（移動）の検証
    - [x] ファイル/フォルダを別のフォルダにドラッグした際、ホバーされたフォルダがハイライトされること
      → 対策完了：draggingPathが空の時の早期リターンとガードを実装し、ドラッグ時の動作を安定化。再テスト待ち。
      → 再テストOK
    - [x] フォルダをドロップした際、そのフォルダ配下に正しく移動されること
    - [x] ファイルの上にドロップした際、そのファイルの親フォルダ（同一階層）へ移動されること
    - [x] フォルダを「自分自身」または「自分自身の子フォルダ」にドラッグ＆ドロップした際、移動がキャンセルされ、ステータスバー（左下）に赤文字で「自分自身またはサブフォルダへは移動できません」とエラーが表示されること
    - [x] 表示されたエラーメッセージは自動で消えず、保存やファイル選択など、次のステータス更新アクションが発生するまで維持されること
    - [x] ファイルツリー外（エディタ、タブバー等）へドラッグ＆ドロップした際、何も起きずに静かにキャンセルされ、ステータスバーにもエラーが表示されないこと
    - [x] ツリー最下部余白へドロップした際、ルートフォルダ（nce）に正しく移動されること
      → 下部余白部へドラッグしてもファイルがルートフォルダ(nce)へ移動しない
      → 再テストOK
    - [x] 開いているファイルを移動した際、エディタ上のタブが閉じられずに新パスに更新されて編集状態が維持されること

# フォルダ削除時の安全対策 タスクリスト

- [x] `docs/wip/` 配下のドキュメントを `docs/` 直下に移動し、実装フェーズへ移行（ユーザー承認後）
- [ ] バックエンド（Rust）の実装
  - [x] `src/main.rs` に `is_dir_empty_custom` ヘルパー関数を実装
  - [x] `trash_file_or_dir` に空チェックを追加し、エラーを返すようにする
  - [x] `delete_file_or_dir_permanently` に空チェックを追加し、エラーを返すようにする
- [ ] フロントエンド（JS）の実装
  - [x] `src/dist/i18n.js` に警告タイトルと本文メッセージの日本語キーを追加
  - [x] `src/dist/js/ui/sidebar.js` の `deleteItemInTree` にエラー処理とエクスプローラ連携を追加
  - [x] `src/dist/js/ui/sidebar.js` の `deleteItemPermanentlyInTree` に同様のエラー処理とエクスプローラ連携を追加
- [ ] 手動テストと検証
  - [ ] 1. 完全な空フォルダの削除（ゴミ箱移動、完全削除）が正常に行えることの確認
  - [ ] 2. `.DS_Store` や `.gitignore`、`Thumbs.db` のみが存在するフォルダが「空」とみなされて削除できることの確認
  - [ ] 3. テキストファイルや画像ファイルが入っているフォルダの削除がブロックされ、警告ダイアログが表示されることの確認
  - [ ] 4. サブフォルダが1つあるだけでも削除がブロックされることの確認
  - [ ] 5. 警告ダイアログで「はい」を選択した際、対象フォルダがOSのエクスプローラで開くこと、「いいえ」では何も起きないことの確認
- [ ] 実装完了後のクリーンアップ・マージ作業
  - [ ] `docs/history.md` に変更履歴を追記
  - [ ] 変更内容をコミット＆プッシュし、マージ・クリーンアップ指示を待つ
